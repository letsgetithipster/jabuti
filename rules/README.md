# rules/ — núcleo de voz (`00-voz.md`), compilado para o workspace por `gerar_harness.py`; personas por classe chegam na Fase 4b. Fontes do compilador multi-LLM: só o alvo claude-code é gerado hoje. Nenhum dado pessoal entra aqui — a suíte varre.
