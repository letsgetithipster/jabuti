# rules/ — núcleo de voz (`00-voz.md`), compilado para o workspace por `gerar_harness.py`; personas por classe chegam na Fase 4b. Fontes do compilador multi-LLM: só o alvo claude-code é gerado hoje; os outros agentes leem `00-voz.md` direto, trocando os marcadores, como manda o AGENTS.md da raiz. Nenhum dado pessoal entra aqui — a suíte varre.
