# tests/ — suíte pytest do motor. Roda no pre-commit; caminhos que geram número de ordem têm teste obrigatório. Sem rede: providers recebem `buscar` injetável e usam tests/fixtures/cotacoes; extratos usam fixtures sintéticas (tests/fixtures/extratos e tests/extratos_sinteticos.py). Testes de xlsx fazem importorskip('openpyxl'). Os testes de subprocesso levam o marker `slow` (registrado em pytest.ini): o pre-commit roda TODOS, o marker existe para o laço interno — `python -m pytest -m "not slow"`.
